/**
 * App identity for the web layer.
 * Keep APP_VERSION in sync with android/app/build.gradle (versionName).
 */
export const APP_NAME = 'Flow';
export const APP_TAGLINE = 'Your money, in flow.';
export const APP_VERSION = '1.0.0';

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- WebView serving (also merged from the Capacitor library). -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Flow reads the SMS inbox to detect bank/payment transaction messages.
      This is a runtime permission: the app only requests it during onboarding,
      after explaining exactly what it is used for. All parsing happens
      on-device; message content never leaves the phone.
    -->
    <uses-permission android:name="android.permission.READ_SMS" />

    <!--
      Notification listening uses a special-access service (enabled via
      Android Settings, no runtime permission). The service is declared in a
      later phase — nothing to declare here yet.
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- WebView serving (also merged from the Capacitor library). -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Flow reads the SMS inbox to detect bank/payment transaction messages.
      Runtime permission, requested in-app only after an explanation.
      All parsing happens on-device; message content never leaves the phone.
    -->
    <uses-permission android:name="android.permission.READ_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Notification listener: special-access service (enabled by the user in
          Android Settings). Reads only potentially-financial notifications,
          parses them with the same on-device parser as SMS, deduplicates
          against stored transactions and writes to SQLite. Never any network.
        -->
        <service
            android:name=".core.notify.FlowNotificationListener"
            android:label="Flow transaction detection"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>

</manifest>
